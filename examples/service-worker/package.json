{
  "name": "lit-ssr-edge-service-worker-example",
  "version": "1.0.0",
  "description": "Example: lit-ssr-edge SSR inside a browser Service Worker — no server required",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "start": "npm run build && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "lit": "3.3.2",
    "lit-ssr-edge": "file:../.."
  },
  "devDependencies": {
    "esbuild": "0.27.3"
  }
}
